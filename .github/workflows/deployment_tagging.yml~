name: Deployment Version Labeling
run-name: ${{ github.actor }} is tagging the commit for deployment.
on:
  workflow_dispatch:
jobs:
  Tag-Commit-Version:
    name: Tag
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repo
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0 # All history
          fetch-tags: true
          sparse-checkout: | # Checkout empty tree.
      - name:　xawdasd
        run: |
          git
